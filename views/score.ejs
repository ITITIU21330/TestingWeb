<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Score</title>
    <link rel="stylesheet" href="/css/score.css">
    <link rel="stylesheet" href="/css/guage.css">
    <meta property="og:title" content="Kết quả của bạn!" />
    <meta property="og:description" content="Bạn đã đạt <%= score %> điểm! Chia sẻ với bạn bè ngay!" />
    <meta property="og:image" content="<%= imageUrl %>" />
    <meta property="og:url" content="<%= pageUrl %>" />
    <meta property="og:type" content="website" />
</head>
<body>
    <h1>ĐÁNH GIÁ MỨC ĐỘ TRƯỞNG THÀNH<br>VỀ QUẢN TRỊ KHÁCH HÀNG</h1>

    <div class="result-container">
        <div class="score-section">
            <p><strong>SCORE: </strong><span id="score"><%= score %></span></p>
            <p><strong>LEVEL: </strong><span id="level">-</span></p>
        </div>

        <div class="guage-speedometer">
            <div class="guage-label">Ad hoc</div>
            <div class="guage-label">Establishing</div>
            <div class="guage-label">Performing</div>
            <div class="guage-label">Optimizing</div>
            <div class="guage-label">Embedded</div>
            <div class="guage-needle"></div>
        </div>

        <img id="result-icon" src="<%= imageUrl %>" alt="Kết quả" width="100">
        <p id="result-text"></p>
        <p>HƯỚNG GIẢI QUYẾT:</p>
        <ul id="actions"></ul>
        <a id="result-link" href="<%= pageUrl %>" target="_blank" class="cta">Tìm hiểu thêm</a>
        <button class="share-button" onclick="goToSharePage()">Chia sẻ</button>
    </div>

    <script src="/js/score.js"></script>
    <script src="/js/share.js"></script>
</body>
</html>
